server:
  port: 6001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://39.105.7.229:${server.port}/eureka/
spring:
  application:
    name: eureka
